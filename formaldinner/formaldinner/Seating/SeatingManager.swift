//
//  SeatingManager.swift
//  formaldinner
//
//  Created by Spencer Michaels on 2/20/20.
//  Copyright © 2020 cate. All rights reserved.
//

import Foundation

struct seatingManager {
/*
func parseJSON(_ seatingData: Data) -> SeatingModel? {
      let decoder = JSONDecoder()
      do {
        let decodedData = try decoder.decode(SeatingData.self, from: seatingData)
          let first = decodedData.first
          let last = decodedData.last
          let id = decodedData.ID
          
          let seating = SeatingModel(first: first, last: last, id: id)
          return seating
          
      } catch {
        print("error")
        return nil
    }
  }
*/
}
